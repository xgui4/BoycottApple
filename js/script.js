"use strict";

window.addEventListener('DOMContentLoaded',init);

function init()
{	
    document.getElementById("connexion").addEventListener("click", Message)
}

function Message(){
    alert("Le mécanisme de site web dynamique n'est pas supporté par Github Pages.")
}
